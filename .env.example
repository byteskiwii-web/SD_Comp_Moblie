# Copy this file to .env and set it to your dev machine's LAN IP.
# The phone running Expo Go must be on the same Wi-Fi network as this
# machine. Find your IP with `ipconfig` (Windows) — look for "IPv4
# Address" under your Wi-Fi adapter.
EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:3000
